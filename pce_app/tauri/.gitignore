# Rust build artefacts
target/
Cargo.lock

# Tauri-generated files (rebuilt by cargo tauri dev/build)
gen/
WixTools/

# Packaged sidecar binary (produced by pyinstaller before cargo tauri build)
binaries/

# App icons are generated by `cargo tauri icon`; don't check in binaries.
icons/*.png
icons/*.ico
icons/*.icns
!icons/README.md
