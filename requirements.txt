mitmproxy>=10.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
httpx>=0.27.0
mcp>=1.0.0
pystray>=0.19.0
Pillow>=10.0.0
pyinstaller>=6.0.0
selenium>=4.20.0
webdriver-manager>=4.0.0
pytest>=8.0.0

# P1 (TASK-003) — Optional OpenTelemetry exporter.
# PCE works without these installed; the OTLP exporter simply no-ops.
# Install to enable the secondary OTLP channel (see ADR-007).
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.27.0

# P2 (TASK-004) — Optional LiteLLM SDK capture channel.
# PCE works without this; /api/v1/sdk/litellm/start will return
# {"ok": false, "error": "litellm_not_installed"} until the extra is
# installed. Install with `pip install litellm[proxy]` to enable the
# fourth capture channel (see TASK-004 §5.4).
litellm[proxy]>=1.50.0

# P2 — Optional, for stronger CA subject parsing on the cert wizard.
# Without it, we fall back to a regex-based CN extractor (sufficient for
# the mitmproxy CA subject we manage).
cryptography>=43.0.0

# D-3 (PCE Probe) — agent-facing debug API for E2E. Loopback WebSocket
# multiplexer between the agent (pytest / Cascade / Claude / etc.) and
# the running PCE browser extension. See
# Docs/docs/engineering/PCE-PROBE-API.md.
websockets>=12.0
